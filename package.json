{
  "name": "state-machine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "test": "npx jest",
    "start-postgres": "docker run --pull missing --name postgres-1 -p 127.0.0.1:5430:5432 -e POSTGRES_PASSWORD=postgres -d postgres",
    "stop-postgres": "docker stop postgres-1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "@types/uuid": "^8.3.4",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
